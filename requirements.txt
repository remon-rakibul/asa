# ---- Core: LangGraph agent + FastAPI backend (everything needed to run) ----
langgraph>=1.2,<2
langgraph-prebuilt>=1.1,<2
langgraph-checkpoint-postgres>=3.1
langchain-core>=1.4
langchain-ollama>=1.1
langchain-google-genai>=2.0   # optional cloud LLM (LLM_PROVIDER=gemini)

# Postgres (asyncpg for app data; psycopg for the LangGraph checkpointer)
asyncpg>=0.29
psycopg[binary]>=3.2
psycopg-pool>=3.2

# FastAPI backend
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9    # Form body parsing (Twilio webhooks)

# Utilities
emoji>=2.12
pydantic-settings>=2.4
python-dotenv>=1.0
httpx>=0.27               # WhatsApp Cloud API + BD SMS gateway calls

# Auth (admin login)
pyjwt>=2.9
bcrypt>=4.0


# Migrations
alembic>=1.13

# RAG knowledge base (hospital documents → Chroma + Ollama embeddings)
langchain-chroma>=0.2
langchain-text-splitters>=0.3
chromadb>=1.0
pypdf>=4.0            # PDF text extraction for uploaded hospital documents

# Twilio SMS
twilio>=9.0

# LiveKit token minting for the patient-portal browser voice call.
# Lightweight (no media/ML deps) — the FastAPI process only mints JWTs; the
# voice worker's heavy deps stay in requirements-voice.txt.
livekit-api>=1.0

# Optional: error tracking (inert unless SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0

# ---- Optional: local voice worker ----
# Installed separately so the API/CI stays lightweight:
#   pip install -r requirements-voice.txt
# See requirements-voice.txt (livekit, faster-whisper, torch, transformers).
